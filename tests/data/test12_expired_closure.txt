fn make_adder(x) {
    fn add(y) {
        return x + y;
    }
    return add;
}

fn main() {
    let add5 = make_adder(5);
    let result = add5(7); // should fail (functional programming yet to be implemented)
    print(result);
}

main();